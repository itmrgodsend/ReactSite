import React from 'react';
import {Container} from "react-bootstrap";

const Contacts = () => {
    return (
        <div className='wp'>
            hi
        </div>
    );
};

export default Contacts;